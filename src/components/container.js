import styled from "styled-components";

const Container = styled.div`
  position: relative;
  max-width: 1200px;
  margin: 0 auto;
  &.is-full-height {
    height: 100%;
  }
`

export default Container
